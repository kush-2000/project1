---
title: 'Project 1: Wrangling, Exploration, Visualization'
author: "SDS322E"
date: ''
output:
  html_document:
    toc: yes
    toc_float:
      collapsed: no
      smooth_scroll: yes
  pdf_document:
    toc: no
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, eval = TRUE, fig.align = "center", warning = F, message = F,
tidy=TRUE, tidy.opts=list(width.cutoff=60), R.options=list(max.print=100))
```

## Data Wrangling, Exploration, Visualization

### Kush Patel

#### Introduction 

Paragraph or two introducing your datasets and variables, why they are interesting to you, etc.

```{R}
# read your datasets in here, e.g., with read_csv()
library(readr)
library(dplyr)
library(tidyverse)
new_cases <- read_csv("new_cases.csv")
new_deaths <- read_csv("new_deaths.csv")
```

#### Tidying: Reshaping

If your datasets are tidy already, demonstrate that you can reshape data with pivot wider/longer here (e.g., untidy and then retidy). Alternatively, it may be easier to wait until the wrangling section so you can reshape your summary statistics. Note here if you are going to do this.

```{R}
# your tidying code (if applicable; can also wait until wrangling section)
```

    
#### Joining/Merging

```{R}
nrow(new_cases) #no. of rows in this dataset
ncol(new_cases) 
nrow(new_deaths)
ncol(new_deaths)
#full_join(new_cases, new_deaths, by ="date", suffix=c("_cases", "_deaths"))
full_data <- full_join(new_cases, new_deaths, by ="date", suffix=c("_cases", "_deaths"))
nrow(full_data)
ncol(full_data)
anti_join(new_cases, new_deaths, by = "date")

```

Discussions of joining here. Feel encouraged to break up into more than once code chunk and discuss each in turn.

####  Wrangling

```{R}
full_data %>% pivot_longer(cols=-c("date", "World_cases", "World_deaths"), names_to="name", values_to="value") %>% separate(name, sep = "_", into=c("Country","type")) %>% pivot_wider(names_from="type",values_from="value")

clean <- full_data %>% pivot_longer(cols=-c("date", "World_cases", "World_deaths"), names_to="name", values_to="value") %>% separate(name, sep = "_", into=c("Country","type")) %>% pivot_wider(names_from="type",values_from="value")

clean  %>% mutate(ratio = deaths/(cases+1))  %>% na.omit() %>% arrange(desc(ratio))
                  
clean %>% group_by(Country) %>% na.omit() %>% summarize(total_dealth = sum(deaths)) %>% arrange(desc(total_dealth))

clean %>% filter(Country == "China") %>% select(date, Country, cases, deaths)

clean %>% group_by(Country) %>% summarize(mean_cases = round(mean(cases, na.rm =T)), sd_cases = round(sd(cases, na.rm = T ))) 

clean %>% summarize(mean_cases = round(mean(cases, na.rm =T)), n(), n_distinct(Country)) 
clean %>% n_distinct()

clean %>% filter(Country == "United Kingdom") %>%
  mutate(Country2 = str_replace(Country, "United Kingdom", "UK"))


clean %>% mutate(month = format(date, "%m"), year = format(date, "%Y")) %>%
group_by(year, month, Country) %>% summarize(monthy_average = sum(cases, na.rm = T)) 

```

Your discussion of wrangling section here. Feel encouraged to break up into more than once code chunk and discuss each in turn.


#### Visualizing

```{R}
clean %>% group_by(Country) %>% summarize(total_cases=sum(cases, na.rm=T)) %>% arrange(desc(total_cases)) %>% slice(1:7) %>% ggplot(aes(Country,total_cases)) + geom_bar(stat="identity") 

clean %>% group_by(Country) %>% summarize(total_cases=sum(cases, na.rm=T)) %>% arrange(desc(total_cases)) %>% slice(1:7) %>% ggplot(aes(x=reorder(Country,total_cases), y=total_cases)) + geom_bar(stat="identity", fill = "blue") 


```

Your discussion of plot 1

```{R}
clean %>% group_by(Country) %>% summarize(total_cases=sum(cases, na.rm=T), total_deaths=sum(deaths, na.rm = T)) %>% arrange(desc(total_cases)) %>% slice(1:10) %>%  ggplot(aes(total_cases, total_deaths)) + geom_point(aes(color=Country), size=4) 

clean %>% group_by(Country) %>% summarize(total_cases=sum(cases, na.rm=T), total_deaths=sum(deaths, na.rm = T)) %>% arrange(desc(total_cases)) %>% slice(5:15) %>%  ggplot(aes(total_cases, total_deaths)) + geom_point(aes(color=Country), size=4) 
```

Your discussion of plot 2

```{R}
clean %>% mutate(month = format(date, "%m"), year = format(date, "%Y")) %>%
group_by(year, month, Country = "United States" ) %>% summarize(monthy_average_cases = round(mean(cases, na.rm = T))) %>% ggplot(aes(x= month, y=monthy_average_cases)) + geom_bar(stat="identity", fill = "pink") 

 


Italy <- clean %>% mutate(month = format(date, "%m"), year = format(date, "%Y")) %>%
group_by(year, month, Country = "Italy" ) %>% summarize(monthy_average_cases = round(mean(cases, na.rm = T)))
  
ggplot(data = US, aes(x= month, y=monthy_average_cases)) + geom_bar(stat="identity", fill = "pink") 

ggplot(data = Italy, aes(x= month, y=monthy_average_cases)) + geom_point(size=4) + geom_line(stat="identity")

df <- merge(US,Italy,by="month")

ggplot(df, aes(x=month, y=monthy_average_cases.x)) +
  geom_bar(position="stack",stat="identity", fill ="white") + 
  geom_line(aes(x=month, y=monthy_average_cases.y)) + 
  ylab("") + xlab("x")

#--------
US_Italy <- clean %>% mutate(month = format(date, "%m"), year = format(date, "%Y")) %>%
group_by(year, month, Country) %>% filter(Country == "United States" | Country == "Italy") %>% summarize(monthy_average_cases = round(mean(cases, na.rm = T))) 
 
 ggplot(data = US_Italy, aes(x= month, y=monthy_average_cases, fill = Country)) + geom_bar(stat="identity") +facet_grid(Country~.)

```

Your discussion of plot 3

#### Concluding Remarks

If any!




